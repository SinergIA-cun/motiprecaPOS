packages:
  - 'apps/*'
  - 'packages/*'

# Scripts de build aprobados explícitamente (pnpm los bloquea por defecto por seguridad).
# esbuild prepara su binario nativo de plataforma; lo usan Vite y tsx.
allowBuilds:
  esbuild: true
